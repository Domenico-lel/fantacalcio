// Costanti del sistema pronostici/crediti.
// Modulo client/server-safe (niente "use server") così è importabile ovunque.

export const STARTING_CREDITS = 500;

// Bonus partecipazione alla giornata (in base al risultato del proprio scontro)
export const REWARD_WIN = 10;
export const REWARD_DRAW = 4;
export const REWARD_LOSS = 0;
